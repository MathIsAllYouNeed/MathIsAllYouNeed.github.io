// ── Visual configuration for index.html ────────────────────
const TILING_CONFIG = {
  // Rhombus colors: (0,1)=red, (0,2)=yellow, (1,2)=green
  COLORS: ["#e63946", "#f4a261", "#2a9d8f"],

  // Rhombus edge stroke
  STROKE: "#111",
  STROKE_WIDTH: 0.5,

  // Hexagon boundary stroke
  BOUNDARY_STROKE: "#111",
  BOUNDARY_WIDTH: 1.0,

  // Flippable vertex indicators
  FLIP_R: 0.2,            // radius as fraction of S
  FLIP_FILL: "#ffffff", %"#2563eb",
  FLIP_STROKE: "none",
  FLIP_STROKE_WIDTH: 0,
};
